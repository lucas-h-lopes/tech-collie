spring:
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      format_sql: true
  datasource:
    url: jdbc:h2:mem:techcollie
    username: sa
    password:
    name: dev_db
